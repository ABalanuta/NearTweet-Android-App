<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pt.utl.ist.tagus.cmov.neartweet"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="17" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="pt.utl.ist.tagus.cmov.neartweetapp.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pt.utl.ist.tagus.cmov.neartweetapp.NewTweetActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="pt.utl.ist.tagus.cmov.neartweet.NewTweetActivity2"
            android:label="@string/title_activity_new_tweet_activity2" >
        </activity>
        <activity
            android:name="pt.utl.ist.tagus.cmov.neartweet.TweetDetailsActivity"
            android:label="@string/title_activity_tweet_details" >
        </activity>

        <service
            android:name="pt.utl.ist.tagus.cmov.neartweetapp.networking.ConnectionHandlerService"
            android:enabled="true" />
    </application>

</manifest>